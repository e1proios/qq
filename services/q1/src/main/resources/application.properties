# MongoDB connection settings
quarkus.mongodb.connection-string = mongodb://host.docker.internal:27017
quarkus.mongodb.database = game-reviews

# Optional: Credentials (if MongoDB has auth enabled)
# quarkus.mongodb.credentials.username=admin
# quarkus.mongodb.credentials.password=secret
